.boxes {
	margin: 20px 0 0;
	display: flex;
	flex-wrap: wrap;
	justify-content: space-between;

	.box {
		width: 48%;
		margin: 0;
		margin-bottom: 4%;
		display: flex;
		border: 1px solid #ddd;
		border-radius: 2px;
		transition: .2s ease-in-out;

		&:hover {
			border-color: #bcbcbc;
		}
	}
}